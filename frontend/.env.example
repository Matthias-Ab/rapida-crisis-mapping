# API base URL (default: /api/v1 proxied via Vite dev server)
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Dashboard API key (can also be entered at runtime in the dashboard login screen)
VITE_DASHBOARD_KEY=your-dashboard-key-here
